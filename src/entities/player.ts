export class Player {
    index: number;
    name: string;
    color: string;

    constructor(index: number, name: string, color: string) {
        this.index = index;
        this.name = name;
        this.color = color;
    }
}
